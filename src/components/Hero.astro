---
---

<section id="hero" class="hero" aria-label="Introduction">
  <div class="container hero-inner">

    <!-- Left: copy -->
    <div class="hero-copy">
      <p class="greeting fade-up d1">Hello, I'm</p>
      <h1 class="name fade-up d2">
        Vashok<span class="dot">.</span>
      </h1>
      <p class="role fade-up d3">
        <span class="role-text">Data Engineer</span>
      </p>
      <p class="bio fade-up d4">
        I design and build <strong>scalable data pipelines</strong>, cloud-native ETL
        workflows, and observability systems that transform petabytes of raw data into
        reliable, actionable insights. Currently engineering mission-critical data
        infrastructure at <a href="#experience"><strong>Comcast</strong></a>.
      </p>
      <div class="actions fade-up d5">
        <a href="#projects" class="btn btn-primary">View My Work</a>
        <a href="/assets/resume.pdf" class="btn btn-outline" download>
          <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24"
               fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round"
               stroke-linejoin="round" aria-hidden="true">
            <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/>
            <polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/>
          </svg>
          Resume
        </a>
      </div>
      <div class="socials fade-up d5">
        <a href="https://github.com/vijayashreeua-afk" target="_blank" rel="noopener noreferrer"
           aria-label="GitHub profile">
          <svg xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24"
               fill="currentColor" aria-hidden="true">
            <path d="M12 0C5.37 0 0 5.37 0 12c0 5.3 3.438 9.8 8.205 11.385.6.113.82-.258.82-.577
                     0-.285-.01-1.04-.015-2.04-3.338.724-4.042-1.61-4.042-1.61-.546-1.385-1.335-1.755
                     -1.335-1.755-1.087-.744.084-.729.084-.729 1.205.084 1.838 1.236 1.838 1.236
                     1.07 1.835 2.809 1.305 3.495.998.108-.776.417-1.305.76-1.605-2.665-.3-5.466
                     -1.332-5.466-5.93 0-1.31.465-2.38 1.235-3.22-.135-.303-.54-1.523.105-3.176
                     0 0 1.005-.322 3.3 1.23.96-.267 1.98-.399 3-.405 1.02.006 2.04.138 3 .405
                     2.28-1.552 3.285-1.23 3.285-1.23.645 1.653.24 2.873.12 3.176.765.84 1.23
                     1.91 1.23 3.22 0 4.61-2.805 5.625-5.475 5.92.42.36.81 1.096.81 2.22
                     0 1.606-.015 2.896-.015 3.286 0 .315.21.69.825.57C20.565 21.795 24 17.295
                     24 12c0-6.63-5.37-12-12-12z"/>
          </svg>
        </a>
        <a href="https://www.linkedin.com/in/vijayashree-ashok/" target="_blank" rel="noopener noreferrer"
           aria-label="LinkedIn profile">
          <svg xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24"
               fill="currentColor" aria-hidden="true">
            <path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136
                     1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85
                     3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433a2.062 2.062 0 0 1-2.063-2.065
                     2.064 2.064 0 1 1 2.063 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771
                     C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227
                     24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/>
          </svg>
        </a>
        <a href="mailto:vijayashreeua@gmail.com" aria-label="Send email">
          <svg xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24"
               fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"
               stroke-linejoin="round" aria-hidden="true">
            <rect x="2" y="4" width="20" height="16" rx="2"/>
            <path d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7"/>
          </svg>
        </a>
      </div>
    </div>

    <!-- Right: decorative code block -->
    <div class="hero-visual fade-up d3" aria-hidden="true">
      <div class="code-window">
        <div class="code-titlebar">
          <span class="dot red"></span>
          <span class="dot yellow"></span>
          <span class="dot green"></span>
          <span class="code-filename">data_engineer.py</span>
        </div>
        <pre class="code-body"><code><span class="kw">class</span> <span class="cls">DataEngineer</span>:
    name     = <span class="str">"Vashok"</span>
    location = <span class="str">"Philadelphia, PA"</span>
    company  = <span class="str">"Comcast"</span>

    stack = [
        <span class="str">"PySpark"</span>, <span class="str">"Databricks"</span>,
        <span class="str">"AWS S3"</span>, <span class="str">"Python"</span>,
        <span class="str">"MongoDB"</span>, <span class="str">"Snowflake"</span>,
    ]

    <span class="kw">def</span> <span class="fn">build</span>(self, data):
        <span class="kw">return</span> Pipeline(
            extract=data,
            transform=self.optimize,
            load=<span class="str">"prod"</span>,
        )</code></pre>
      </div>
    </div>

  </div>

  <!-- Scroll hint -->
  <a href="#about" class="scroll-hint fade-up d5" aria-label="Scroll to about section">
    <span class="scroll-label">Scroll</span>
    <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24"
         fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"
         stroke-linejoin="round" aria-hidden="true">
      <path d="M12 5v14M5 12l7 7 7-7"/>
    </svg>
  </a>
</section>

<style>
.hero {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  justify-content: center;
  padding: 7rem 0 5rem;
  position: relative;
  overflow: hidden;
}

/* Subtle background grid */
.hero::before {
  content: '';
  position: absolute;
  inset: 0;
  background-image:
    linear-gradient(var(--border) 1px, transparent 1px),
    linear-gradient(90deg, var(--border) 1px, transparent 1px);
  background-size: 60px 60px;
  mask-image: radial-gradient(ellipse 80% 60% at 50% 50%, black 40%, transparent 100%);
  pointer-events: none;
}

.hero-inner {
  display: grid;
  grid-template-columns: 1fr 1fr;
  align-items: center;
  gap: 4rem;
  position: relative;
  z-index: 1;
}

/* Copy */
.greeting {
  font-family: var(--font-mono);
  font-size: 1rem;
  color: var(--accent);
  letter-spacing: 0.05em;
  margin-bottom: 0.25rem;
}
.name {
  font-size: clamp(3rem, 8vw, 5.5rem);
  font-weight: 800;
  line-height: 1.05;
  letter-spacing: -0.03em;
  color: var(--text-primary);
  margin-bottom: 0.4rem;
}
.dot { color: var(--accent); }

.role {
  font-size: clamp(1.1rem, 2.5vw, 1.4rem);
  color: var(--text-secondary);
  font-weight: 500;
  margin-bottom: 1.5rem;
}
.role-text {
  background: linear-gradient(90deg, var(--accent), var(--accent-warm));
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  font-weight: 700;
}

.bio {
  color: var(--text-secondary);
  font-size: 1.05rem;
  max-width: 520px;
  margin-bottom: 2rem;
  line-height: 1.8;
}
.bio a { font-weight: 600; color: var(--accent); }
.bio a:hover { color: var(--accent-warm); }

.actions {
  display: flex;
  gap: 1rem;
  flex-wrap: wrap;
  margin-bottom: 2rem;
}

.socials {
  display: flex;
  gap: 0.75rem;
}
.socials a {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 42px;
  height: 42px;
  border: 1px solid var(--border);
  border-radius: var(--radius-sm);
  color: var(--text-secondary);
  transition: border-color var(--transition), color var(--transition), background var(--transition);
}
.socials a:hover {
  border-color: var(--accent);
  color: var(--accent);
  background: var(--accent-glow);
}

/* Code window */
.code-window {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  overflow: hidden;
  box-shadow: 0 20px 60px rgba(0, 0, 0, 0.4);
  font-family: var(--font-mono);
  font-size: 0.88rem;
}
.code-titlebar {
  display: flex;
  align-items: center;
  gap: 6px;
  padding: 0.8rem 1rem;
  background: var(--bg-secondary);
  border-bottom: 1px solid var(--border);
}
.dot { border-radius: 50%; width: 12px; height: 12px; }
.dot.red    { background: #ff5f56; }
.dot.yellow { background: #ffbd2e; }
.dot.green  { background: #27c93f; }
.code-filename {
  margin-left: 0.5rem;
  color: var(--text-muted);
  font-size: 0.8rem;
}
.code-body {
  padding: 1.5rem;
  overflow-x: auto;
  line-height: 1.85;
  white-space: pre;
}
.kw  { color: #c792ea; }
.cls { color: #82aaff; }
.fn  { color: #82aaff; }
.str { color: #c3e88d; }

/* Scroll hint */
.scroll-hint {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 0.4rem;
  color: var(--text-muted);
  font-size: 0.78rem;
  font-family: var(--font-mono);
  letter-spacing: 0.08em;
  text-transform: uppercase;
  margin-top: 3rem;
  animation: bounce 2s ease infinite;
  position: relative;
  z-index: 1;
}
.scroll-hint:hover { color: var(--accent); }

@keyframes bounce {
  0%, 100% { transform: translateY(0); }
  50%       { transform: translateY(6px); }
}

@media (max-width: 900px) {
  .hero-inner { grid-template-columns: 1fr; gap: 2.5rem; }
  .hero-visual { display: none; }
}
</style>
